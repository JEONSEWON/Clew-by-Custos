# Clew (by Custos) — 프로젝트 진단표

## 제품 진단 (누구의 · 어떤 문제)
멀티에이전트 시스템을 운영하는 개발자·소규모 팀이, 에이전트 *사이*에서 발생하는 토큰 낭비(중복 루프·핑퐁 핸드오프·재조회)가 얼마이고 어디서 새는지 보지 못한다. 기존 관측 도구는 단일 에이전트용이라 이 "사이 층"을 못 본다. Clew는 트레이스를 받아 낭비 지점과 추정 토큰/비용을 리포트로 짚는다.

## 현재 산출물 (실제 존재하는 것)
- 동작하는 캐스케이드 탐지기(구조→의미), 3개 낭비 패턴: repeat_node · pingpong · requery_known
- python -m clew analyze CLI + markdown/JSON 리포트 렌더러, save/load 파이프라인
- OTel/OpenInference JSON 입력(Format A) — LangGraph·CrewAI·AutoGen·LlamaIndex·PydanticAI 커버
- 합성 검증 GO(F1 0.8571, FP 0), pytest 171개 통과, 태그: real-probe-v1 등
- 실측 프로브: 실 Haiku 트레이스 5종에서 4개 탐지 경로 작동 확인

## 기술 진단 (Track A)
- 테스트: pytest 171개 통과
- CLI: python -m clew analyze 동작
- 합성 성능: F1 0.8571 / FP 0 (TP·FP·TN·FN = 30·0·40·10), φ=0.514345 동결
- E3 (핵심 결함): 의미 레이어가 실데이터에서 분리력 상실 — 비낭비 쌍 above-φ 비율 ~100%(코사인 0.59~1.0). FP 0은 구조 레이어 단독 기여이며 의미 필터 덕분이 아님. 단 n=1 토픽·5트레이스 관찰이라 확정 아님.
- 미검증(솔직히): 실측 검증 · 실측 절감액 · 유료 고객 = 모두 아직 0
- 해당 없음: build/lint/typecheck · RLS · .env 보안항목 (Python CLI 프로젝트라 Web 보안 항목 비해당)

## 4주 목표
- Must: 실측 외부 트레이스 3~5건 확보 → φ 재보정 실험(별도 사전등록, 예시에 맞춘 hand-fit 금지)
- Should: 통증 대화 5건 이상 / 실측 절감액 케이스 1건("X팀 월 $Y 낭비 발견") — Must 두 개 성공 시 시도하는 스트레치 목표
- Won't (이번 4주에 절대 안 함): 합성 데이터 추가 정제 / 의미 레이어 즉석 재설계(1토픽 오버핏 금지) / 실시간 차단(S1) / n8n·Dify 어댑터 / 회사 홈페이지

## 성공 기준 (확인 가능한 문장)
- 외부 사용자로부터 실제 트레이스 3~5건이 리포에 수집된다
- 통증 대화 5건 이상이 기록된다
- 재검토 조건: 20+ 접촉에 트레이스 0건이면 아웃리치 채널을 재평가한다

## 가장 큰 리스크 (1개 특정)
시장 리스크 — 실측 트레이스 수집 실패. 남은 핵심 질문(φ 전이·실데이터 의미 분리)은 합성 작업으로 답할 수 없고 오직 외부 실측 트레이스로만 풀린다. 현재 콜드 아웃리치는 사전등록 임계(20접촉)에 한참 못 미치고 응답 0. 랩에서 탐지 정밀화를 계속하는 것이 더 어려운 수요 검증의 편한 대체재가 되는 것이 진짜 함정.

## CLAUDE.md 효과 검증 (O3)

같은 요청을 CLAUDE.md 없는 빈 폴더 / CLAUDE.md 있는 리포 루트에서 각각 실행해 결과를 5항목으로 비교.
- 요청(양쪽 동일): "멀티에이전트 트레이스에서 '토큰 낭비 패턴'을 하나 더 탐지하는 기능을 추가해줘. 어디에 어떤 파일을 만들지 네가 판단해서 진행해."
- 실험 방식: 관찰 전용. 리포 측 편집은 승인하지 않고 계획만 확인, 실험 후 git restore로 코드 원상복구(동결 엔진 보호).

| 항목 | 기준 없음 (빈 폴더) | CLAUDE.md 있음 (리포 루트) |
|---|---|---|
| 파일 위치 | 결정 못 함 — 어디 만들지 되물음 | src/clew/detect/structural.py·cascade.py에 정확히 배치 |
| 타입 안정성 | 코드 생성 안 함 | type hint 완비 (trace: Trace -> list[Span]) |
| 구조 경계 | 기존 패턴·구조 모름, 되물음 | 캐스케이드 2단 구조(structural→cascade) 준수, span_kind 구분 |
| 도메인/누수 가드 | 도메인 개념 없음 | tool 스팬 제외 등 도메인 지식 반영 |
| 검증(pytest) | 언급 없음 | tests/test_cascade.py에 테스트 추가 계획 |

관찰 요약: CLAUDE.md 없으면 도메인·폴더·기존 패턴을 몰라 질문부터 하고 멈춘다. CLAUDE.md 있으면 되묻지 않고 정확한 파일 위치·타입·구조·도메인·검증까지 갖춘 계획을 즉시 제시한다. 5개 항목 전부에서 명확한 차이 확인.
